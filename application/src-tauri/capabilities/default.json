{
  "identifier": "default",
  "description": "Default permissions for mdviewer",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "fs:default"
  ]
}
